export enum userFormLabels {
    first_name = "first_name",
    last_name = "last_name",
    email = "email",
    phone = "phone",
    gender = "gender",
}

export enum userDetailsAPIParams {
    getTypes = 'zac',
    getDetails = 'srinivas'
}

export enum drawerPlacement {
    left = 'left',
    right = 'right',
    top = 'top',
    bottom = 'bottom'
}

export enum operationType {
    create = 'create',
    update = 'update'
}

export enum genderOptions {
    male = "male",
    female = "female",
    other = "other"
}